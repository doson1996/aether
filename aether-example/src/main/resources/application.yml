server:
  port: 8083
  servlet:
    context-path: /api

spring:
  application:
    name: aether-executor

aether:
  server:
    host: localhost:8081
    context-path: /api
  client:
    #name: executor1
    #host: localhost:8082
